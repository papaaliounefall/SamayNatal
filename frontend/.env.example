# Base URL of the Django API (no trailing slash). Point this at the
# Docker-mapped backend port (see docker-compose.yml) or your local
# `manage.py runserver` port.
VITE_API_BASE_URL=http://localhost:8010
